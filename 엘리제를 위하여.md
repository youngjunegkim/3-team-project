
### 프로젝트 개요

이 코드는 EV3 브릭으로 '엘리제를 위하여' 도입부를 연주하는 프로그램이다. 음악의 박자에 맞춰 **Port D 모터**를 좌우로 교차 회전시켜 입모양을 움직이는 기능도 추가해 보았다.

---

###  1. 하드웨어 구성 및 포트

| 부품 | 역할 | 연결 포트 |
| :--- | :--- | :--- |
| **EV3 Brick** | 메인 허브, 연주 제어 | - |
| **Performer Motor** | 댄스 모터 | **Port D** |

---

###  2. Python 코드 전문

```python
#!/usr/bin/env pybricks-micropython
from pybricks.hubs import EV3Brick
from pybricks.ev3devices import Motor
from pybricks.parameters import Port, Stop
from pybricks.tools import wait
from pybricks.parameters import Direction

# EV3 브릭 객체 생성
ev3 = EV3Brick()
performer_motor = Motor(Port.D)

# --- 모터 제어 변수 ---
MOTOR_STEP_ANGLE = 10 
motor_toggle_state = 1 # 1: 정방향, -1: 역방향
MOTOR_SPEED = 600

# --- 음표 주파수 정의 (Hz) ---
C3 = 131 
G3 = 196
A3 = 220 
B3 = 247 

C4 = 262 
D4 = 294 
E4 = 330 
F4 = 349 
G4 = 392 
A4 = 440 
B4 = 494 

C5 = 523 
D5 = 587 
Ds5 = 622 
E5 = 659 
G5 = 784 

# --- 박자 정의 (ms) ---
TEMPO_BASE = 100 
SIXTEENTH = TEMPO_BASE     
EIGHTH = TEMPO_BASE * 2    
QUARTER = TEMPO_BASE * 4   
DOT_EIGHTH = TEMPO_BASE * 3

# --- 도우미 함수 ---
def play_note(note_freq, duration, with_motor=True):
    
    global motor_toggle_state 
    
    ev3.speaker.beep(note_freq, duration)
    
    if with_motor:
        angle = MOTOR_STEP_ANGLE * motor_toggle_state 
        
        performer_motor.run_angle(
            speed=MOTOR_SPEED, 
            rotation_angle=angle, 
            then=Stop.BRAKE, 
            wait=False 
        )
        
        motor_toggle_state *= -1 # 다음 음표를 위해 방향 반전
    
    wait(duration + 5) 

def play_chord(note_list, duration):
    """화음 연주 (모터 움직임 없음)."""
    for note in note_list:
        ev3.speaker.beep(note, duration)
    wait(duration + 5)


# --- 메인 실행 함수 ---
def main():
    """엘리제를 위하여 멜로디와 화음을 재생하며 모터를 움직인다."""
    global motor_toggle_state 
    
    ev3.screen.clear()
    ev3.screen.print("--- Fur Elise Score Player ---")
    ev3.speaker.say("Starting the full performance") 
    
    # 모터 초기화
    performer_motor.run_angle(
        speed=300, 
        rotation_angle=-performer_motor.angle(), 
        then=Stop.HOLD, 
        wait=True
    )
    motor_toggle_state = 1
    
    # --- 도입부 (A 섹션) ---
    
    # 마디 1~2
    play_note(E5, SIXTEENTH)
    play_note(Ds5, SIXTEENTH)
    play_note(E5, SIXTEENTH)
    play_note(Ds5, SIXTEENTH)
    play_note(E5, SIXTEENTH)
    
    # 마디 3
    play_note(B4, SIXTEENTH)
    play_note(D5, SIXTEENTH)
    play_note(C5, SIXTEENTH)
    
    play_note(A4, DOT_EIGHTH) 
    play_chord([A3, C4], DOT_EIGHTH) 
    
    # 마디 4
    play_note(E4, SIXTEENTH) 
    play_note(A4, SIXTEENTH)
    play_note(B4, SIXTEENTH)
    
    # 마디 5
    play_note(C5, SIXTEENTH)
    play_note(E5, SIXTEENTH) 
    
    play_note(A4, DOT_EIGHTH)
    play_chord([C4, D4], DOT_EIGHTH) 

    # 마디 6~10 (반복 시작)
    play_note(E5, SIXTEENTH)
    play_note(Ds5, SIXTEENTH)
    play_note(E5, SIXTEENTH)
    play_note(Ds5, SIXTEENTH)
    play_note(E5, SIXTEENTH)
    
    play_note(B4, SIXTEENTH)
    play_note(D5, SIXTEENTH)
    play_note(C5, SIXTEENTH)
    
    play_note(A4, DOT_EIGHTH)
    play_chord([G3, D4], DOT_EIGHTH) 
    
    play_note(E5, SIXTEENTH)
    play_note(G5, SIXTEENTH)
    play_note(F4, SIXTEENTH) 
    play_note(E4, SIXTEENTH) 
    
    play_note(D5, SIXTEENTH)
    play_note(F4, SIXTEENTH) 
    play_note(E4, SIXTEENTH) 
    play_note(D4, DOT_EIGHTH) 
    
    # 마디 11~14
    play_note(C4, DOT_EIGHTH)
    play_chord([G3, C4], DOT_EIGHTH)
    
    play_note(B3, DOT_EIGHTH)
    play_chord([G3, B3], DOT_EIGHTH)

    play_note(A3, DOT_EIGHTH)
    play_chord([C3, F4], DOT_EIGHTH)
    play_note(A3, DOT_EIGHTH)
    play_chord([C3, F4], DOT_EIGHTH)
    
    # 마디 15~16
    play_note(E5, SIXTEENTH)
    play_note(Ds5, SIXTEENTH)
    play_note(E5, SIXTEENTH)
    play_note(Ds5, SIXTEENTH)
    play_note(E5, SIXTEENTH)
    
    # 마디 17
    play_note(B4, SIXTEENTH)
    play_note(D5, SIXTEENTH)
    play_note(C5, SIXTEENTH)
    
    # 마디 18
    play_note(A4, DOT_EIGHTH) 
    play_chord([A3, C4], DOT_EIGHTH) 
    
    # 마디 19
    play_note(E4, SIXTEENTH) 
    play_note(A4, SIXTEENTH)
    play_note(B4, SIXTEENTH)
    
    # 마디 20 (1번 반복)
    play_note(C5, SIXTEENTH)
    play_note(E5, SIXTEENTH) 
    
    play_note(A4, DOT_EIGHTH)
    play_chord([G3, D4], DOT_EIGHTH) 
    
    ev3.screen.print("--- Performance Complete (A Section) ---")

# 메인 함수 실행
if __name__ == "__main__":
    main()